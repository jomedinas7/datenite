class Restaurant{
  var name;
  var phone;
  var website;
  var id;
  var cuisine; // list of the type of foods served
  var address;
  var menu;//of type List of type Food? Maybe just the parsedBody and turn into menu later

  Restaurant(this.name, this.phone,
      this.website, this.id,this.cuisine,
      this.address, this.menu);



}